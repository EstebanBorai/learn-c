#include <stdio.h>

int main() {
    // Loop For:
    // 1. Counter
    // 2. Condition
    // 3. Increment/Control Variable
    for (int counter = 5; counter <= 20; counter += 2) {
        printf("El for va por la iteracion: %d\n", counter);
    }
}
